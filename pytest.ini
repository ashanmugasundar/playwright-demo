[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
addopts = -q

markers =
    unit: unit tests (fast, no external dependencies)
    api: api tests (django client / DRF)
    e2e: end-to-end tests (playwright)